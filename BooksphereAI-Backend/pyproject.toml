[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
src = ["src"]

[tool.mypy]
mypy_path = "src"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "src"]
addopts = "--cov=booksphere --cov-report=term-missing --cov-fail-under=90"
